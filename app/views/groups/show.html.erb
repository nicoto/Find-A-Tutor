<h2><%= @group.name %></h2>
<h2><%= @group.id %></h2>
<h4><%= @group.description %></h4>

<%= link_to 'Edit', edit_group_path(@group.id) %> | <%= link_to 'Delete', group_path(@group.id), method: :delete, data: {confirm: 'Are you sure?'} %>
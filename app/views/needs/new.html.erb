<%= form_for :need, url: needs_path do |f| %>
  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </p>
  <p>
    <%= f.label :subject %>
    <%= f.select(:subject_id, @subject_options)%>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
